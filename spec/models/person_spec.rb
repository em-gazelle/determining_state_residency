require 'rails_helper'

RSpec.describe Person, type: :model do
	# let(:pe)

	# describe 'leap_year' do
	# 	before each do
	# 		Person.new(year: year)
	# 	end
	# 	it 'returns false when not divisible by 4' do
	# 		expect(Person.new(year: 2011).leap_year).to be false
	# 	end
	# 	it 'returns false when divisible by 4 & 100, but not by 400' do
	# 		expect(person.new(year: 2100).leap_year).to be_false
	# 	end
	# 	it 'returns true when divisible by 4, 100, and 400' do
	# 		expect(person.new(year: 2000).leap_year).to be_true
	# 	end
	# 	it 'returns true when divisible by 4, but not by 100' do
	# 		expect(person.new(year: 2016).leap_year).to be_true
	# 	end
	# end 

end