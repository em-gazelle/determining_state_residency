<h2>All Trips Entered for <%= @year_analysis.year %>:</h2>

<table class="table">
	<% @trips.each do |t| %>
		<tr>
			<td><%= t.state %></td>
			<td><%= t.start_date %></td>
			<td><%= t.end_date %></td>
			<td><%= t.total_days %></td>
		</tr>
	<% end %>
</table>

<%#= link_to "Convert to CSV" %>